[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "assistant"
version = "0.1.0"
description = "Custom middleware and tooling layered onto vendored dcode."
requires-python = ">=3.12,<4.0"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["assistant"]
